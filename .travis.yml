language: node_js
node_js:
  - '9'
branches:
  only:
  - master
install:
  - npm install -g firebase-tools
  - npm install -g @angular/cli@1.7.3
script:
  - npm install
  - npm run build
after_success:
  - firebase deploy